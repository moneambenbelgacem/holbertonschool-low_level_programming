#include "main.h"
/**
 * _isupper - function that print negative or positif check
 *
 * @c: variable input
 * Return:  1 or 0 is_upper.
 */
int _isupper(int c)
{
	if((c >= 65)&&(c <= 90))
	return (1);
	return (0);
}