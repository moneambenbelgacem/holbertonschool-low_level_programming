#include "main.h"

/**
 * _strlen - update the value of n
 *@s: value input
 * Return: Always 0.
 */
int _strlen(char *s)
{ 

int count = 0 ;

while(s[count] != '\0')
{
	count++;
	

}
return (count);


}